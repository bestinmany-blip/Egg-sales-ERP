{
  "name": "commercial-invoicing-app",
  "version": "2.0.0",
  "description": "Cross-platform commercial invoicing, inventory, and accounting web application built with Capacitor.",
  "main": "index.html",
  "scripts": {
    "start": "npx http-server ./ -p 8080",
    "build": "echo 'Web assets ready in root directory.'",
    "cap:add:android": "cap add android",
    "cap:sync": "cap sync",
    "cap:open:android": "cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^5.0.0",
    "@capacitor/core": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}
